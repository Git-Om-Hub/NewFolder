#include <stdio.h>

int main() {
    int l,b;
    printf("Enter the length of a rectangle: \n");
    scanf("%d", &l);
    printf("Enter the breadth of a rectangle: \n");
    scanf("%d", &b);
    printf("The number is: %d", l*b);
    return 0;
}
